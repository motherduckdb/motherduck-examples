dbt_churn_prediction:
  outputs:
    local:
      type: duckdb
      path: local.db
      schema: main
      threads: 4
    prod:
      type: duckdb
      path: "md:{{ env_var('MOTHERDUCK_DATABASE', 'subscription_churn') }}"
      schema: main
      threads: 4
  target: local
