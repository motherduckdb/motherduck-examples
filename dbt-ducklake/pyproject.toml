[project]
name = "dbt-ducklake"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dbt-core==1.9.9",
    "flask>=3.1.1",
    "duckdb>=1.3.2",
    "dbt-duckdb==1.9.6",
]
