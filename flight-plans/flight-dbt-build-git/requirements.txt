# dbt + a DuckDB client for `md:` connect and the run_results snapshot INSERT.
# dbt-duckdb pulls in `duckdb` transitively; the `dbt` console script comes from
# dbt-core. git is NOT listed — the Flights runtime preinstalls the binary.
dbt-core>=1.8.0,<2.0.0
dbt-duckdb>=1.8.0,<2.0.0
duckdb>=1.0.0
