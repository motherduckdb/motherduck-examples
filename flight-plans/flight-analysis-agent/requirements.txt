duckdb==1.5.3
# Pydantic AI runs the agent loop and speaks OpenRouter's OpenAI-compatible API,
# so the agent is model-agnostic: swap the MODEL slug for any tool-capable model.
# Pinned for reproducible Flight builds. The [openrouter] extra provides
# OpenRouterModel and OpenRouterProvider. The weather tool uses stdlib urllib,
# so no HTTP client dependency is needed.
pydantic-ai-slim[openrouter]==2.2.0
