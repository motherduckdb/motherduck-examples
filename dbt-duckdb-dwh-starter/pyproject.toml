[project]
name = "commoncrawl-motherduck-dbt"
version = "0.1.0"
description = "Minimal dbt-duckdb project for Common Crawl domain graph and HackerNews analysis on MotherDuck."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "dbt-duckdb>=1.9,<2.0",
]
